# Тестовый Next.js проект с Prisma + PostgreSQL

## 📋 Что создано

Создан тестовый проект `my-prisma-postgres-app` для проверки подключения к базе данных.

## 🎯 Цель

Проверить, что:
- ✅ Connection string работает
- ✅ Prisma Client генерируется
- ✅ Подключение к БД устанавливается
- ✅ Можно применять миграции

## 📁 Структура проекта

```
my-prisma-postgres-app/
├── .env                    # Connection string из основного проекта
├── prisma/
│   └── schema.prisma      # Схема Prisma
├── app/                    # Next.js App Router
├── package.json
└── ...
```

## 🚀 Использование

### 1. Перейти в проект

```bash
cd my-prisma-postgres-app
```

### 2. Проверить подключение

```bash
. "$HOME/.nvm/nvm.sh"
npx prisma db pull
```

### 3. Открыть Prisma Studio

```bash
npm run db:studio
```

Откроется на http://localhost:5555

### 4. Применить миграцию из основного проекта

#### Вариант A: Через SQL напрямую

Скопируйте SQL из `../prisma/migrations/add_new_features.sql` и выполните в Prisma Studio или через SQL Editor.

#### Вариант B: Через Prisma Migrate

```bash
# Создать новую миграцию на основе SQL файла
npx prisma migrate dev --name add_new_features --create-only

# Затем отредактировать созданный файл миграции
# И применить
npx prisma migrate deploy
```

### 5. Запустить Next.js приложение

```bash
npm run dev
```

Откроется на http://localhost:3000

## 🔧 Настройка

### DATABASE_URL

Connection string уже скопирован из основного проекта `.env` файла.

Если нужно обновить:

```bash
# Отредактировать .env
nano .env
```

Или:

```bash
echo 'DATABASE_URL="postgres://..."' > .env
```

## ✅ Проверка работы

### 1. Проверить подключение

```bash
npx prisma db pull
```

Должно показать существующие таблицы в БД.

### 2. Открыть Prisma Studio

```bash
npm run db:studio
```

Должен открыться веб-интерфейс с данными из БД.

### 3. Применить миграцию

Скопируйте SQL из `../prisma/migrations/add_new_features.sql` и выполните через:
- Prisma Studio (SQL Editor)
- Vercel SQL Editor
- Или через `psql`

## 🗑️ Удаление тестового проекта

Если проект больше не нужен:

```bash
cd ..
rm -rf my-prisma-postgres-app
```

## 📚 Полезные команды

```bash
# Генерация Prisma Client
npx prisma generate

# Открыть Prisma Studio
npm run db:studio

# Применить миграции
npx prisma migrate deploy

# Создать новую миграцию
npx prisma migrate dev --name migration_name

# Синхронизировать schema с БД
npx prisma db push
```

## 🎯 Следующие шаги

1. ✅ Проект создан
2. ✅ Зависимости установлены
3. ✅ DATABASE_URL настроен
4. ⏳ Проверить подключение: `npx prisma db pull`
5. ⏳ Применить миграцию из основного проекта
6. ⏳ Запустить тестовое приложение: `npm run dev`

