# Шаги после создания базы данных в Vercel

## ✅ Текущая ситуация

Вы видите страницу базы данных **"prisma-postgres-lime-house"** в Vercel:
- ✅ База данных создана
- ✅ Статус: "Доступный" (Available)
- ✅ Видны переменные окружения: `PRISMA_DATABASE_URL`, `POSTGRES_URL`

---

## 📋 Что делать дальше (по порядку)

### Шаг 1: Скопировать переменные окружения

1. **На вкладке ".env.local"** вы видите переменные:
   ```
   PRISMA_DATABASE_URL="*************"
   POSTGRES_URL="*************"
   ```

2. **Нажмите "Показать секрет"** (Show secret) — чтобы увидеть реальные значения

3. **Нажмите "Копировать фрагмент"** (Copy fragment) — скопируйте все переменные

4. **Добавьте в Vercel Environment Variables**:
   - Перейдите в **Settings** → **Environment Variables**
   - Добавьте переменную `DATABASE_URL` со значением из `PRISMA_DATABASE_URL`
   - Или добавьте все три переменные:
     - `DATABASE_URL` = значение из `PRISMA_DATABASE_URL`
     - `PRISMA_DATABASE_URL` = (скопированное значение)
     - `POSTGRES_URL` = (скопированное значение)

   **Важно**: Код использует `DATABASE_URL`, поэтому добавьте её обязательно!

---

### Шаг 2: Подключить проект к базе данных

1. **Нажмите кнопку "Подключить проект"** (Connect project) справа вверху

2. В модальном окне:
   - Выберите проект **"sadaka2025"**
   - Выберите все окружения: Development, Preview, Production
   - **Префикс**: оставьте пустым или введите "DATABASE"
   - Нажмите **"Соединять"** (Connect)

3. После подключения переменные автоматически добавятся в проект

---

### Шаг 3: Применить миграцию БД

После подключения нужно применить миграцию, чтобы создать таблицы:

#### Способ 1: Через Vercel SQL Editor (если доступен)

1. На этой же странице базы данных найдите вкладку **"SQL"** или **"Query"**
2. Откройте файл `prisma/migrations/add_new_features.sql` в вашем проекте
3. Скопируйте весь SQL код
4. Вставьте в SQL Editor
5. Нажмите **Execute** или **Run**

#### Способ 2: Через Prisma Studio (если установлен)

```bash
# В локальной машине с установленным Node.js
npx prisma studio
```

#### Способ 3: Через внешний SQL клиент

1. Используйте `PRISMA_DATABASE_URL` для подключения
2. Откройте файл `prisma/migrations/add_new_features.sql`
3. Выполните SQL команды

---

### Шаг 4: Проверить применение миграции

После применения миграции проверьте, что таблицы созданы:

**В SQL Editor выполните:**

```sql
-- Проверить таблицу payments
SELECT table_name FROM information_schema.tables WHERE table_name = 'payments';

-- Проверить колонку role в users
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'role';

-- Проверить таблицу zakat_calculations
SELECT table_name FROM information_schema.tables 
WHERE table_name = 'zakat_calculations';
```

Если все запросы возвращают результаты — миграция применена успешно! ✅

---

## 🔧 Настройка переменных окружения в Vercel

### Обязательные переменные для проекта

После подключения БД добавьте в **Settings** → **Environment Variables**:

```
# База данных (должна быть автоматически после подключения)
DATABASE_URL=postgresql://...

# API
VITE_API_BASE_URL=https://bot.e-replika.ru/api/v1
VITE_API_TOKEN=test_token_123

# Insan API
VITE_INSAN_API_URL=https://fondinsan.ru/api/v1
VITE_INSAN_ACCESS_TOKEN=0xRs6obpvPOx4lkGLYxepBOcMju

# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш-токен-бота

# Платежные шлюзы (когда получите ключи)
YOOKASSA_SHOP_ID=ваш-shop-id
YOOKASSA_SECRET_KEY=ваш-secret-key
CLOUDPAYMENTS_PUBLIC_ID=ваш-public-id
CLOUDPAYMENTS_SECRET_KEY=ваш-secret-key
```

---

## ✅ Чеклист действий

- [ ] Скопировал переменные из ".env.local" вкладки
- [ ] Добавил `DATABASE_URL` в Vercel Environment Variables
- [ ] Нажал "Подключить проект" и подключил к sadaka2025
- [ ] Применил миграцию через SQL Editor
- [ ] Проверил, что таблицы созданы
- [ ] Добавил остальные переменные окружения (API токены, платежи)

---

## 🎯 Итоговая последовательность

1. **Сейчас**: Скопируйте `PRISMA_DATABASE_URL` и добавьте как `DATABASE_URL` в Environment Variables
2. **Затем**: Нажмите "Подключить проект" → выберите sadaka2025 → все окружения → "Соединять"
3. **После**: Примените миграцию через SQL Editor
4. **Проверка**: Убедитесь, что таблицы созданы
5. **Финал**: Добавьте остальные переменные окружения (API токены)

---

## 🆘 Если не видите SQL Editor

Если в интерфейсе Vercel нет SQL Editor:

1. **Используйте Prisma Studio** (локально):
   ```bash
   npx prisma studio
   ```
   Откроется веб-интерфейс для работы с БД

2. **Используйте внешний клиент**:
   - pgAdmin
   - DBeaver
   - TablePlus
   - Подключитесь используя `PRISMA_DATABASE_URL`

3. **Через Vercel CLI**:
   ```bash
   vercel env pull .env.local
   # Затем используйте DATABASE_URL для подключения
   ```

---

## 📝 Важно помнить

- `DATABASE_URL` — это то, что использует ваш код
- `PRISMA_DATABASE_URL` — специальная переменная для Prisma
- После подключения проекта переменные добавятся автоматически
- Но лучше добавить `DATABASE_URL` вручную для совместимости

