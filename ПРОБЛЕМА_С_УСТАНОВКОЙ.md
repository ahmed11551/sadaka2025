# Проблема с установкой зависимостей

## ❌ Текущая проблема

**Сетевые ошибки при установке npm пакетов** (`ECONNRESET`)

Это временная проблема с интернет-соединением или npm registry.

## 🔍 Что обнаружено

1. ✅ Node.js установлен: v24.11.1
2. ✅ npm установлен: v11.6.2
3. ✅ MONGODB_URI настроен в `.env`
4. ❌ npm пакеты не устанавливаются из-за сетевых ошибок
5. ⚠️ **Важно**: На скриншоте MongoDB Atlas видно "Создать кластер" - кластер может быть не создан!

## 🎯 Две проблемы

### Проблема 1: Сетевые ошибки npm
**Решение**: Дождаться восстановления сети или использовать другой registry

### Проблема 2: Кластер MongoDB может быть не создан
**Решение**: Создать кластер в MongoDB Atlas

## 📋 Что делать

### Шаг 1: Создать кластер в MongoDB Atlas

На скриншоте видно кнопку **"Создать кластер"** (Create Cluster). Это нужно сделать в первую очередь!

1. В MongoDB Atlas нажмите **"Создавать"** (зелёная кнопка)
2. Выберите:
   - **Поставщик**: AWS, Google Cloud или Azure
   - **Регион**: Выберите ближайший (например, Europe)
   - **Тип**: M0 (Free tier) для начала
3. Нажмите **"Создать кластер"**
4. Дождитесь создания (5-10 минут)

### Шаг 2: Получить connection string

После создания кластера:
1. Нажмите **"Connect"** на кластере
2. Выберите **"Connect your application"**
3. Скопируйте connection string
4. Обновите `MONGODB_URI` в `.env`

### Шаг 3: Настроить Network Access

1. В MongoDB Atlas перейдите в **"Security" → "Network Access"**
2. Нажмите **"Add IP Address"**
3. Выберите **"Allow Access from Anywhere"** (0.0.0.0/0) для тестирования
   - Или добавьте ваш IP адрес

### Шаг 4: Установить зависимости (когда сеть восстановится)

```bash
. "$HOME/.nvm/nvm.sh"
npm install
```

### Шаг 5: Проверить подключение

```bash
npm run db:mongo:test
```

## 🔧 Альтернативные решения

### Если сеть не восстанавливается

1. **Использовать другой npm registry**:
   ```bash
   npm config set registry https://registry.npmmirror.com
   npm install
   ```

2. **Установить только mongodb**:
   ```bash
   npm install mongodb --no-save
   ```

3. **Использовать yarn**:
   ```bash
   npm install -g yarn
   yarn install
   ```

## ⚠️ Важно

**Перед проверкой подключения убедитесь, что:**
1. ✅ Кластер создан в MongoDB Atlas
2. ✅ Network Access настроен (IP whitelist)
3. ✅ Connection string правильный
4. ✅ Зависимости установлены

## 📊 Текущий статус

- **Кластер MongoDB**: ⚠️ Нужно создать (на скриншоте видно "Создать кластер")
- **Зависимости**: ❌ Не установлены (проблема с сетью)
- **Connection string**: ✅ Настроен в `.env`
- **Код**: ✅ Готов к использованию

## 🎯 Приоритет действий

1. **Сначала**: Создать кластер в MongoDB Atlas
2. **Затем**: Настроить Network Access
3. **Потом**: Установить зависимости (когда сеть восстановится)
4. **В конце**: Проверить подключение

---

**Создайте кластер в MongoDB Atlas - это критично для работы!**

