# Команды для применения миграции через CLI

## 🎯 Если Node.js установлен

### Вариант 1: Prisma Migrate Deploy (для production)

```bash
# Применить все неприменённые миграции
npx prisma migrate deploy
```

### Вариант 2: Prisma Migrate Dev (для development)

```bash
# Создать новую миграцию из schema.prisma и применить
npx prisma migrate dev --name add_new_features
```

### Вариант 3: Prisma DB Push (быстрый способ, без миграций)

```bash
# Синхронизировать schema.prisma с БД напрямую
npx prisma db push
```

---

## 🎯 Если PostgreSQL клиент установлен (psql)

```bash
# Применить SQL файл напрямую
psql $DATABASE_URL < prisma/migrations/add_new_features.sql

# Или с явным указанием URL
psql "postgresql://user:password@host:port/database" < prisma/migrations/add_new_features.sql
```

---

## 🎯 Через Docker (если используете)

```bash
# Применить миграцию в контейнере
docker-compose exec postgres psql -U sadakapass -d sadakapass < prisma/migrations/add_new_features.sql

# Или через Prisma в контейнере
docker-compose exec app npx prisma migrate deploy
```

---

## 🎯 Через Vercel (рекомендуется, так как БД там)

### Способ 1: SQL Editor в Vercel

1. Vercel Dashboard → Storage → prisma-postgres-lime-house
2. Откройте SQL Editor
3. Скопируйте содержимое `prisma/migrations/add_new_features.sql`
4. Вставьте и выполните

### Способ 2: Vercel CLI + Prisma

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите
vercel login

# 3. Подключите проект
vercel link

# 4. Получите переменные окружения
vercel env pull .env.local

# 5. Примените миграцию
npx prisma migrate deploy
```

---

## ✅ Проверка после применения

```bash
# Через Prisma Studio (визуальный интерфейс)
npx prisma studio

# Или через SQL запрос
psql $DATABASE_URL -c "SELECT table_name FROM information_schema.tables WHERE table_name = 'payments';"
```

---

## 📋 Текущая ситуация

**В вашей системе**:
- ❌ Node.js не установлен
- ❌ PostgreSQL клиент (psql) не установлен
- ✅ База данных создана в Vercel
- ✅ DATABASE_URL есть в .env

**Рекомендация**: Используйте **Vercel SQL Editor** — это самый простой способ, так как БД уже там создана.

---

## 🚀 Быстрое решение

1. Откройте Vercel Dashboard
2. Storage → prisma-postgres-lime-house
3. SQL Editor
4. Скопируйте `prisma/migrations/add_new_features.sql`
5. Выполните

Готово! ✅

