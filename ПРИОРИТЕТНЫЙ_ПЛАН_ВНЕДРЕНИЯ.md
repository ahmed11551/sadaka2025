# Приоритетный план внедрения функций

## ✅ Что уже готово

1. **Структура бэкенда** (100%):
   - ✅ Платежные сервисы (YooKassa, CloudPayments)
   - ✅ Модерация кампаний (контроллеры, middleware)
   - ✅ Отчёты (сервис, контроллер)
   - ✅ Подписки (сервис, cron job)
   - ✅ Telegram bot (сервис, контроллер)
   - ✅ Админ-панель (контроллеры)

2. **UI Frontend** (70%):
   - ✅ Быстрые платежи (форма готова)
   - ✅ Подписки (UI планов на главной, вкладка в профиле)
   - ✅ Калькулятор закята (полная страница)
   - ✅ Кампании (форма создания, просмотр)
   - ✅ История (страница есть, но моковые данные)
   - ✅ Фонды-партнёры (полная страница)

3. **Миграции БД** (готовы):
   - ✅ SQL миграции созданы

---

## 🎯 Приоритетный план (по важности)

### 🔥 КРИТИЧНО (Неделя 1-2)

#### 1. Интеграция платежных шлюзов (3-4 дня)
**Цель**: Реальные платежи вместо симуляции

**Задачи**:
- [ ] Обновить Prisma schema (добавить модель Payment)
- [ ] Применить миграцию БД
- [ ] Интегрировать `PaymentService` в `DonationService`
- [ ] Обновить `DonationModal` для инициализации платежа через `/api/payments/initiate`
- [ ] Добавить страницы `/payment/success` и `/payment/failed`
- [ ] Настроить webhooks для YooKassa и CloudPayments
- [ ] Добавить роуты в `routes.ts`
- [ ] Настроить env переменные (YOOKASSA_SHOP_ID, CLOUDPAYMENTS_PUBLIC_ID)

**Результат**: Пользователи могут реально платить, платежи обрабатываются через шлюзы

---

#### 2. Модерация кампаний (2-3 дня)
**Цель**: Кампании на модерации перед публикацией

**Задачи**:
- [ ] Обновить Prisma schema (добавить moderationStatus в Campaign)
- [ ] Применить миграцию БД
- [ ] Обновить `CampaignService.createCampaign` (статус по умолчанию: pending)
- [ ] Создать страницу `/admin/campaigns` для модерации
- [ ] Добавить роуты в `routes.ts`
- [ ] Создать первого админа в БД

**Результат**: Новые кампании требуют одобрения админа

---

#### 3. Подписки с рекуррентными платежами (3-4 дня)
**Цель**: Реальные подписки с автосписанием

**Задачи**:
- [ ] Обновить Prisma schema (добавить поля для рекуррентов в Subscription)
- [ ] Применить миграцию БД
- [ ] Интегрировать `SubscriptionService` в процесс оплаты подписки
- [ ] Обновить UI подписок на главной - подключить к API
- [ ] Добавить страницу управления подписками (pause/resume/cancel)
- [ ] Настроить cron job для автосписания (или Vercel Cron)
- [ ] Добавить роуты в `routes.ts`

**Результат**: Пользователи могут оформить подписку, она автоматически списывается

---

### ⚙️ ВАЖНО (Неделя 2-3)

#### 4. История и отчёты (3-4 дня)
**Цель**: Реальная история вместо моков, экспорт отчётов

**Задачи**:
- [ ] Подключить страницу `/history` к реальному API
- [ ] Реализовать фильтры по типу/периоду
- [ ] Добавить графики с реальными данными
- [ ] Интегрировать CSV экспорт из `ReportService`
- [ ] Добавить кнопки экспорта в UI
- [ ] Добавить роуты в `routes.ts`

**Результат**: Пользователи видят свою реальную историю и могут экспортировать отчёты

---

#### 5. Админ-панель UI (2-3 дня)
**Цель**: Удобный интерфейс для админов

**Задачи**:
- [ ] Создать страницу `/admin` с дашбордом
- [ ] Страница `/admin/campaigns` для модерации
- [ ] Страница `/admin/stats` со статистикой
- [ ] Добавить навигацию для админов
- [ ] Добавить проверку роли пользователя

**Результат**: Админы могут управлять платформой через UI

---

### 💡 ЖЕЛАТЕЛЬНО (Неделя 3-4)

#### 6. Inline-режим Telegram (2-3 дня)
**Цель**: Команды бота для быстрого доступа

**Задачи**:
- [ ] Установить `telegraf` или настроить webhook
- [ ] Настроить обработку команд (/sadaqa, /zakat, /campaigns, /partners)
- [ ] Настроить webhook URL в Telegram Bot API
- [ ] Протестировать команды

**Результат**: Пользователи могут использовать команды бота

---

#### 7. Улучшения UI/UX (2-3 дня)
**Цель**: Полировка интерфейса

**Задачи**:
- [ ] Улучшить анимации и переходы
- [ ] Добавить загрузочные состояния везде
- [ ] Улучшить мобильную версию
- [ ] Добавить уведомления о важных событиях

**Результат**: Более плавный и приятный UX

---

## 📋 Чек-лист готовности к каждой задаче

### Перед началом работы:
- [ ] Обновить Prisma schema
- [ ] Применить миграции БД (`npx prisma migrate dev`)
- [ ] Обновить `routes.ts` с новыми эндпойнтами
- [ ] Настроить env переменные
- [ ] Протестировать локально

---

## 🔧 Инструкции по внедрению

### Шаг 1: Обновить Prisma Schema

Добавить в `prisma/schema.prisma`:

```prisma
// Payment model
model Payment {
  id          String   @id @default(uuid())
  donationId  String   @unique
  donation    Donation @relation(fields: [donationId], references: [id], onDelete: Cascade)
  provider    String   // "yookassa" | "cloudpayments"
  amount      Decimal  @db.Decimal(15, 2)
  currency    String   @default("RUB")
  status      String   @default("pending")
  providerId  String?
  paymentUrl  String?
  metadata    Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

// Update User
model User {
  role        String   @default("user") // "user" | "admin" | "moderator"
}

// Update Campaign
model Campaign {
  moderationStatus String @default("pending")
  moderationNote   String?
  moderatedAt      DateTime?
  moderatedBy      String?
}

// Update Subscription
model Subscription {
  providerId      String?
  providerToken   String?
  lastChargeDate  DateTime?
  chargeAttempts  Int      @default(0)
  maxChargeAttempts Int    @default(3)
}
```

### Шаг 2: Применить миграцию

```bash
npx prisma migrate dev --name add_payment_and_moderation
# или использовать готовый SQL
psql $DATABASE_URL < prisma/migrations/add_new_features.sql
```

### Шаг 3: Обновить routes.ts

Добавить новые роуты (см. файлы контроллеров для деталей).

### Шаг 4: Настроить env переменные

Добавить в `.env` и Vercel:
- `YOOKASSA_SHOP_ID`
- `YOOKASSA_SECRET_KEY`
- `CLOUDPAYMENTS_PUBLIC_ID`
- `CLOUDPAYMENTS_SECRET_KEY`
- `TELEGRAM_BOT_TOKEN`
- `SITE_URL`

---

## ⏱️ Временные оценки

| Задача | Время | Приоритет |
|--------|-------|-----------|
| Платежи | 3-4 дня | 🔥 КРИТИЧНО |
| Модерация | 2-3 дня | 🔥 КРИТИЧНО |
| Подписки | 3-4 дня | 🔥 КРИТИЧНО |
| История/Отчёты | 3-4 дня | ⚙️ ВАЖНО |
| Админ-панель | 2-3 дня | ⚙️ ВАЖНО |
| Telegram | 2-3 дня | 💡 ЖЕЛАТЕЛЬНО |
| UI улучшения | 2-3 дня | 💡 ЖЕЛАТЕЛЬНО |

**Итого**: 17-24 дня работы (3-4 недели)

---

## 🎯 Рекомендации

1. **Начните с платежей** - без них проект не работает
2. **Модерация вторым** - безопасность контента
3. **Подписки третьим** - основной доход
4. **Остальное по возможности** - улучшает UX, но не блокирует

---

## ✅ Метрики успеха

После каждой задачи:
- [ ] Функция работает локально
- [ ] Функция работает на staging (Vercel)
- [ ] Добавлены базовые тесты
- [ ] Документация обновлена

После всех критичных задач:
- [ ] Пользователи могут реально платить
- [ ] Кампании модерируются
- [ ] Подписки работают
- [ ] Проект готов к тестированию с реальными пользователями

