# Полная инструкция: Создание Telegram бота и настройка Mini App

## 📋 Оглавление

1. [Создание Telegram бота через @BotFather](#1-создание-telegram-бота)
2. [Настройка Mini App](#2-настройка-mini-app)
3. [Добавление URL приложения](#3-добавление-url-приложения)
4. [Настройка меню бота](#4-настройка-меню-бота)
5. [Интеграция с приложением](#5-интеграция-с-приложением)
6. [Настройка Webhook (опционально)](#6-настройка-webhook-опционально)
7. [Тестирование](#7-тестирование)

---

## 1. Создание Telegram бота

### Шаг 1.1: Найти BotFather

1. Откройте Telegram
2. Найдите `@BotFather` в поиске
3. Нажмите **Start** или отправьте `/start`

### Шаг 1.2: Создать нового бота

1. Отправьте команду `/newbot`
2. BotFather спросит: **"Alright, a new bot. How are we going to call it? Please choose a name for your bot."**
   - Введите название бота (например: `MubarakWay Садака-Пасс`)
   - Это название будет видно пользователям

3. BotFather спросит: **"Good. Now let's choose a username for your bot. It must end in `bot`. Like this, for example: TetrisBot or tetris_bot."**
   - Введите username для бота (например: `mubarakway_sadaka_bot`)
   - Username должен быть уникальным и заканчиваться на `bot`
   - Может содержать только латинские буквы, цифры и подчеркивания

4. BotFather выдаст:
   ```
   Done! Congratulations on your new bot. You will find it at t.me/mubarakway_sadaka_bot. You can now add a description, about section and profile picture for your bot, see /help for a list of commands.

   Use this token to access the HTTP API:
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

### Шаг 1.3: Сохранить токен бота

⚠️ **ВАЖНО**: Сохраните токен бота в безопасном месте! Он выглядит как:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Рекомендации**:
- Сохраните токен в `.env` файл (НЕ коммитьте в Git!)
- Добавьте в Vercel Environment Variables для production
- Никогда не публикуйте токен в открытом доступе

---

## 2. Настройка Mini App

### Шаг 2.1: Открыть настройки бота

1. Отправьте команду `/mybots` в чат с @BotFather
2. Выберите вашего бота из списка
3. Выберите **"Bot Settings"**
4. Выберите **"Menu Button"**

### Шаг 2.2: Настроить Menu Button

BotFather спросит: **"What text should appear on the menu button?"**

1. Введите название кнопки (например: `🎁 Садака-Пасс` или `Открыть приложение`)
2. Затем BotFather спросит: **"What URL should open when the button is pressed?"**

---

## 3. Добавление URL приложения

### Шаг 3.1: Указать URL вашего приложения

Введите URL вашего Mini App:

**Для Vercel**:
```
https://sadaka2025.vercel.app
```

**Или для локальной разработки** (через ngrok или аналогичный сервис):
```
https://your-ngrok-url.ngrok.io
```

**Важно**:
- URL должен начинаться с `https://` (не `http://`)
- URL должен быть доступен из интернета
- Для локальной разработки используйте ngrok, localtunnel или аналоги

### Шаг 3.2: Подтвердить настройку

BotFather ответит:
```
Success! The menu button has been updated.
```

Теперь в вашем боте появится кнопка внизу чата, которая открывает Mini App!

---

## 4. Настройка меню бота

### Шаг 4.1: Добавить описание бота

1. Отправьте `/setdescription` в чат с @BotFather
2. Выберите вашего бота
3. Введите описание (например: `Благотворительная платформа MubarakWay. Пожертвования, закят, кампании.`)

### Шаг 4.2: Добавить About секцию

1. Отправьте `/setabouttext`
2. Выберите вашего бота
3. Введите текст о боте (например: `Ваш путь к благому. Пожертвования, закят, подписки, кампании.`)

### Шаг 4.3: Добавить команды бота

1. Отправьте `/setcommands`
2. Выберите вашего бота
3. Введите список команд, например:
   ```
   start - Начать работу с ботом
   donate - Сделать пожертвование
   campaigns - Посмотреть кампании
   zakat - Калькулятор закята
   profile - Мой профиль
   help - Помощь
   ```

### Шаг 4.4: Добавить иконку бота (опционально)

1. Отправьте `/setuserpic`
2. Выберите вашего бота
3. Отправьте изображение (512x512 пикселей, квадратное)

---

## 5. Интеграция с приложением

### Шаг 5.1: Добавить токен бота в приложение

Добавьте токен бота в `.env` файл:

```bash
# .env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

Или в Vercel Environment Variables:
```
TELEGRAM_BOT_TOKEN=ваш-токен-бота
```

### Шаг 5.2: Настроить Telegram WebApp в коде

В вашем приложении нужно обработать данные от Telegram WebApp:

**Пример интеграции** (`client/src/lib/telegram.ts`):

```typescript
// Получение данных от Telegram WebApp
export function initTelegramWebApp() {
  if (typeof window !== 'undefined' && window.Telegram?.WebApp) {
    const tg = window.Telegram.WebApp;
    
    // Инициализация
    tg.ready();
    tg.expand();
    
    // Включить вибрацию
    tg.enableClosingConfirmation();
    
    return tg;
  }
  return null;
}

// Получение данных пользователя из Telegram
export function getTelegramUser() {
  const tg = window.Telegram?.WebApp;
  if (tg?.initDataUnsafe?.user) {
    return tg.initDataUnsafe.user;
  }
  return null;
}

// Получение initData для проверки на сервере
export function getTelegramInitData() {
  const tg = window.Telegram?.WebApp;
  return tg?.initData || '';
}
```

### Шаг 5.3: Добавить скрипт Telegram WebApp в index.html

Обновите `client/index.html`:

```html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no" />
    
    <!-- Telegram WebApp Script -->
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    
    <!-- Остальные теги -->
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

### Шаг 5.4: Инициализировать Telegram WebApp в приложении

В `client/src/main.tsx` или `client/src/App.tsx`:

```typescript
import { initTelegramWebApp } from '@/lib/telegram';

// При загрузке приложения
useEffect(() => {
  const tg = initTelegramWebApp();
  if (tg) {
    // Настроить тему
    tg.setHeaderColor('#3E5F43');
    tg.setBackgroundColor('#ffffff');
    
    // Получить данные пользователя
    const user = getTelegramUser();
    if (user) {
      console.log('Telegram User:', user);
      // Авторизовать пользователя через Telegram ID
    }
  }
}, []);
```

---

## 6. Настройка Webhook (опционально)

Webhook нужен для получения обновлений от пользователей (сообщения, нажатия кнопок).

### Шаг 6.1: Подготовить endpoint для webhook

В вашем backend (`server/routes.ts` или отдельный файл):

```typescript
import express from 'express';

const router = express.Router();

// Webhook endpoint для Telegram
router.post('/webhook/telegram', express.json(), (req, res) => {
  const update = req.body;
  
  // Обработка сообщений
  if (update.message) {
    const chatId = update.message.chat.id;
    const text = update.message.text;
    
    // Логика обработки команд
    if (text === '/start') {
      // Отправить приветственное сообщение
    } else if (text === '/campaigns') {
      // Отправить ссылку на кампании
    }
  }
  
  // Обязательно ответить OK
  res.status(200).json({ ok: true });
});
```

### Шаг 6.2: Установить webhook через Telegram API

**Вариант 1: Через curl**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-backend-url.com/api/webhook/telegram"
  }'
```

**Вариант 2: Через браузер**

Откройте в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-backend-url.com/api/webhook/telegram
```

**Вариант 3: Через код**

```typescript
async function setWebhook() {
  const response = await fetch(
    `https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/setWebhook`,
    {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        url: 'https://your-backend-url.com/api/webhook/telegram'
      })
    }
  );
  
  const data = await response.json();
  console.log('Webhook set:', data);
}
```

### Шаг 6.3: Проверить webhook

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

---

## 7. Тестирование

### Шаг 7.1: Протестировать бота

1. Найдите вашего бота в Telegram (по username)
2. Нажмите **Start**
3. Проверьте, что появилась кнопка внизу чата (Menu Button)
4. Нажмите на кнопку - должно открыться ваше приложение

### Шаг 7.2: Протестировать Mini App

1. Проверьте, что приложение открывается корректно
2. Проверьте, что данные пользователя получаются из Telegram
3. Проверьте, что все функции работают

### Шаг 7.3: Проверить команды

Отправьте команды боту:
- `/start` - должно сработать
- `/help` - должно показать список команд
- Другие команды из вашего списка

---

## 📝 Полезные команды BotFather

| Команда | Описание |
|---------|----------|
| `/mybots` | Список ваших ботов |
| `/token` | Получить токен бота |
| `/revoke` | Отозвать токен (создать новый) |
| `/setname` | Изменить имя бота |
| `/setdescription` | Изменить описание бота |
| `/setabouttext` | Изменить About секцию |
| `/setuserpic` | Изменить иконку бота |
| `/setcommands` | Настроить команды |
| `/deletebot` | Удалить бота |

---

## 🔐 Безопасность

### ⚠️ Важные моменты:

1. **Никогда не публикуйте токен бота**:
   - Не коммитьте в Git
   - Не публикуйте в открытом доступе
   - Храните только в `.env` или переменных окружения

2. **Проверяйте initData на сервере**:
   - Telegram отправляет `initData` в запросах
   - Проверяйте подпись на сервере перед использованием

3. **Используйте HTTPS**:
   - Mini App требует HTTPS
   - Webhook также должен быть на HTTPS

---

## 🛠️ Решение проблем

### Проблема: Кнопка Menu Button не появляется

**Решение**:
- Убедитесь, что URL начинается с `https://`
- Проверьте, что URL доступен из интернета
- Попробуйте отправить `/setmenubutton` снова

### Проблема: Mini App не открывается

**Решение**:
- Проверьте консоль браузера на ошибки
- Убедитесь, что скрипт `telegram-web-app.js` загружен
- Проверьте, что приложение работает на указанном URL

### Проблема: Webhook не работает

**Решение**:
- Проверьте, что endpoint доступен из интернета
- Убедитесь, что используется HTTPS
- Проверьте логи сервера на ошибки
- Используйте `getWebhookInfo` для диагностики

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API документация](https://core.telegram.org/bots/api)
- [Telegram Mini Apps документация](https://core.telegram.org/bots/webapps)
- [Telegram Bot Father](https://t.me/BotFather)
- [Примеры Mini Apps](https://core.telegram.org/bots/webapps#examples)

---

## ✅ Чек-лист готовности

- [ ] Бот создан через @BotFather
- [ ] Токен бота сохранен в `.env`
- [ ] Menu Button настроен с URL приложения
- [ ] Описание и About секция добавлены
- [ ] Команды настроены
- [ ] Telegram WebApp скрипт добавлен в `index.html`
- [ ] Интеграция с Telegram WebApp в коде
- [ ] Webhook настроен (если нужен)
- [ ] Приложение протестировано
- [ ] Все работает корректно

---

**Готово!** Ваш Telegram бот настроен и готов к использованию. 🚀

