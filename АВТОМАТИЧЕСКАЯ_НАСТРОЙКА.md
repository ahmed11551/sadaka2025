# Автоматическая настройка MongoDB

## ✅ Что создано

1. **Скрипт автоматической настройки**: `scripts/auto-setup-mongodb.js`
2. **Команда npm**: `npm run db:mongo:setup`

## 🚀 Использование

### Автоматическая настройка и проверка:

```bash
. "$HOME/.nvm/nvm.sh"
npm run db:mongo:setup
```

Скрипт автоматически:
1. ✅ Проверяет connection string
2. ✅ Подключается к MongoDB
3. ✅ Тестирует запись/чтение/удаление
4. ✅ Создаёт все индексы для всех коллекций
5. ✅ Выводит отчёт о готовности

## 📋 Что делает скрипт

### 1. Проверка подключения
- Проверяет `MONGODB_URI` в `.env`
- Подключается к кластеру
- Тестирует базовые операции

### 2. Создание индексов
Автоматически создаёт индексы для:
- ✅ users
- ✅ campaigns
- ✅ donations
- ✅ partners
- ✅ payments
- ✅ subscriptions
- ✅ zakat_calculations
- ✅ favorites
- ✅ comments

### 3. Отчёт о готовности
Выводит полный отчёт о состоянии MongoDB.

## ⚠️ Если подключение не работает

Скрипт автоматически определит проблему и даст инструкции:

### Проблема: Connection timeout
**Решение**: Настроить Network Access в MongoDB Atlas

### Проблема: Authentication failed
**Решение**: Проверить пароль в connection string

## 🎯 После успешной настройки

Когда скрипт завершится успешно:

```bash
# Запустить сервер
npm run dev

# Или проверить подключение
npm run db:mongo:test
```

## ✅ Преимущества

- **Автоматизация**: Всё в одной команде
- **Проверка**: Автоматическая диагностика проблем
- **Индексы**: Создаются автоматически
- **Отчёт**: Полная информация о состоянии

---

**Используйте: `npm run db:mongo:setup` для полной автоматической настройки!** 🚀

