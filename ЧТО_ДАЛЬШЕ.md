# Что дальше? План действий

## ✅ Что уже сделано

1. ✅ MongoDB репозитории созданы для всех моделей (9 репозиториев)
2. ✅ Подключение к MongoDB настроено (`server/db/mongodb.ts`)
3. ✅ Сервер обновлён для автоматического подключения
4. ✅ Скрипт создания индексов готов (`server/db/init-indexes.ts`)
5. ✅ Примеры контроллеров созданы
6. ✅ Документация написана
7. ✅ `MONGODB_URI` добавлен в `.env`
8. ✅ `mongodb` добавлен в `package.json`

## 🎯 Следующие шаги (по приоритету)

### 1. Установить зависимости ⚡ (когда сеть восстановится)

```bash
. "$HOME/.nvm/nvm.sh"
npm install
```

Это установит:
- `mongodb` - драйвер для MongoDB
- Все остальные зависимости проекта

**Статус**: ⏳ Ожидание восстановления сети

---

### 2. Создать индексы MongoDB 📊

После установки зависимостей:

```bash
# Вариант 1: Через переменную окружения (при запуске сервера)
CREATE_INDEXES=true npm run dev

# Вариант 2: Через отдельную команду
npm run db:mongo:indexes
```

Это создаст все необходимые индексы для быстрого поиска.

**Статус**: ⏳ Ожидание установки зависимостей

---

### 3. Интегрировать MongoDB в существующие сервисы 🔄

#### Вариант A: Заменить Prisma на MongoDB (рекомендуется)

Обновить сервисы для использования MongoDB репозиториев:

**Файлы для обновления:**
- `server/services/auth.service.ts` → использовать `UserRepositoryMongo`
- `server/services/campaign.service.ts` → использовать `CampaignRepositoryMongo`
- `server/services/donation.service.ts` → использовать `DonationRepositoryMongo`
- `server/services/partner.service.ts` → использовать `PartnerRepositoryMongo`
- И т.д. для всех сервисов

**Пример:**

```typescript
// Было (Prisma):
import { UserRepository } from '../repositories/user.repository.js';
const userRepo = new UserRepository();

// Стало (MongoDB):
import { UserRepositoryMongo } from '../repositories/user.repository.mongo.js';
const userRepo = new UserRepositoryMongo();
```

#### Вариант B: Использовать оба (гибридный подход)

Можно использовать MongoDB для новых функций, а Prisma оставить для существующих.

**Статус**: 📝 Требуется решение

---

### 4. Протестировать подключение 🧪

После установки зависимостей и создания индексов:

```bash
# Запустить сервер
npm run dev

# Проверить логи - должно быть:
# ✅ MongoDB подключена
# ✅ Индексы MongoDB созданы (если CREATE_INDEXES=true)
```

**Статус**: ⏳ Ожидание установки зависимостей

---

### 5. Миграция данных (если есть данные в PostgreSQL) 📦

Если у вас уже есть данные в PostgreSQL и нужно перенести в MongoDB:

1. **Экспортировать данные из PostgreSQL**:
   ```bash
   # Используя Prisma или прямой SQL
   ```

2. **Преобразовать в формат MongoDB**:
   - Конвертировать UUID в ObjectId (или оставить как строки)
   - Преобразовать даты
   - Обработать связи между документами

3. **Импортировать в MongoDB**:
   ```typescript
   // Скрипт для импорта
   import { connectToMongoDB, getDB } from './db/mongodb.js';
   const db = getDB();
   await db.collection('users').insertMany(usersData);
   ```

**Статус**: ⏳ Опционально (если есть данные для миграции)

---

### 6. Обновить роуты (если нужно) 🛣️

Если создали новые контроллеры (`.mongo.ts`), нужно обновить `server/routes.ts`:

```typescript
// Использовать MongoDB контроллеры
import { AuthControllerMongo } from './controllers/auth.controller.mongo.js';
import { CampaignControllerMongo } from './controllers/campaign.controller.mongo.js';

const authController = new AuthControllerMongo();
const campaignController = new CampaignControllerMongo();
```

**Статус**: 📝 Готово к обновлению

---

### 7. Настроить переменные окружения в Vercel 🌐

Если деплоите на Vercel:

1. Откройте Vercel Dashboard → Settings → Environment Variables
2. Добавьте `MONGODB_URI`:
   ```
   MONGODB_URI="mongodb+srv://Vercel-Admin-sadaka2025:lTu120QTs6TOVVBr@sadaka2025.f3evghc.mongodb.net/?retryWrites=true&w=majority"
   ```
3. Опционально: `CREATE_INDEXES="true"` (только для первого деплоя)

**Статус**: 📝 Готово к настройке

---

## 📋 Чеклист готовности

### Критичные задачи
- [ ] Установить зависимости: `npm install`
- [ ] Создать индексы: `npm run db:mongo:indexes`
- [ ] Протестировать подключение к MongoDB
- [ ] Интегрировать MongoDB репозитории в сервисы

### Важные задачи
- [ ] Обновить роуты для использования MongoDB контроллеров
- [ ] Настроить переменные окружения в Vercel
- [ ] Протестировать основные функции (регистрация, создание кампаний, пожертвования)

### Опциональные задачи
- [ ] Мигрировать данные из PostgreSQL (если есть)
- [ ] Настроить мониторинг MongoDB
- [ ] Оптимизировать запросы
- [ ] Добавить кэширование (если нужно)

---

## 🚀 Быстрый старт (когда сеть восстановится)

```bash
# 1. Установить зависимости
. "$HOME/.nvm/nvm.sh"
npm install

# 2. Создать индексы
npm run db:mongo:indexes

# 3. Запустить сервер
npm run dev

# 4. Проверить логи
# Должно быть: ✅ MongoDB подключена
```

---

## 🎯 Рекомендуемый порядок действий

1. **Сейчас**: Дождаться восстановления сети
2. **Затем**: Установить зависимости и создать индексы
3. **После**: Интегрировать MongoDB в сервисы
4. **Потом**: Протестировать работу
5. **В конце**: Задеплоить на Vercel

---

## 📚 Полезные файлы

- `MONGODB_ГОТОВО.md` - Полная документация по MongoDB
- `ПРИМЕРЫ_ИСПОЛЬЗОВАНИЯ_MONGODB.md` - Примеры использования
- `server/controllers/auth.controller.mongo.ts` - Пример контроллера
- `server/db/init-indexes.ts` - Скрипт создания индексов

---

## ❓ Вопросы для принятия решений

1. **Полностью заменить Prisma на MongoDB?**
   - ✅ Да → Обновить все сервисы
   - ❌ Нет → Использовать гибридный подход

2. **Есть ли данные для миграции?**
   - ✅ Да → Создать скрипт миграции
   - ❌ Нет → Начать с пустой БД

3. **Когда деплоить на Vercel?**
   - Сразу после тестирования локально
   - После полной интеграции

---

**Всё готово к следующему этапу!** 🎉

