# Как создать тестовый Next.js проект с Prisma

## 🎯 Цель

Создать тестовый проект для проверки подключения к базе данных.

## 📋 Инструкция

### Вариант 1: Автоматическое создание (рекомендуется)

```bash
cd /Users/ahmeddevops/Desktop/SadakaPass_new-main

# Загрузите nvm
. "$HOME/.nvm/nvm.sh"

# Создайте проект (ответьте на вопросы интерактивно)
npx create-next-app@latest my-prisma-postgres-app

# Перейдите в проект
cd my-prisma-postgres-app

# Установите Prisma
npm install prisma @prisma/client

# Инициализируйте Prisma
npx prisma init --datasource-provider postgresql

# Скопируйте DATABASE_URL из основного проекта
# Откройте .env и замените DATABASE_URL на значение из ../.env

# Сгенерируйте Prisma Client
npx prisma generate

# Проверьте подключение
npx prisma db pull
```

### Вариант 2: Ручное создание

```bash
cd /Users/ahmeddevops/Desktop/SadakaPass_new-main

# Создайте директорию
mkdir my-prisma-postgres-app
cd my-prisma-postgres-app

# Инициализируйте npm
npm init -y

# Установите зависимости
npm install next react react-dom prisma @prisma/client

# Инициализируйте Prisma
npx prisma init --datasource-provider postgresql

# Обновите .env с DATABASE_URL из основного проекта
# Скопируйте DATABASE_URL из ../.env

# Сгенерируйте Prisma Client
npx prisma generate

# Проверьте подключение
npx prisma db pull
```

## ✅ Проверка

После создания проекта:

1. **Проверьте подключение**:
   ```bash
   npx prisma db pull
   ```
   Должны увидеть существующие таблицы из БД.

2. **Откройте Prisma Studio**:
   ```bash
   npx prisma studio
   ```
   Откроется на http://localhost:5555

3. **Примените миграцию**:
   Скопируйте SQL из `../prisma/migrations/add_new_features.sql` и выполните через Prisma Studio или Vercel SQL Editor.

## 🔧 Настройка DATABASE_URL

Скопируйте `DATABASE_URL` из основного проекта `.env` файла:

```bash
# В my-prisma-postgres-app/.env
DATABASE_URL="postgres://290ec0599bafdebadd344af8e7abe26e28b739ecdea5b5e54251cfe67f34e5fa:sk_nlc-F6iLMJn8S7id2ySc3@db.prisma.io:5432/postgres?sslmode=require&pool=true"
```

## 📚 Полезные команды

```bash
# Генерация Prisma Client
npx prisma generate

# Открыть Prisma Studio
npx prisma studio

# Применить миграции
npx prisma migrate deploy

# Синхронизировать schema с БД
npx prisma db push
```

## 🗑️ Удаление проекта

Если проект больше не нужен:

```bash
cd /Users/ahmeddevops/Desktop/SadakaPass_new-main
rm -rf my-prisma-postgres-app
```

