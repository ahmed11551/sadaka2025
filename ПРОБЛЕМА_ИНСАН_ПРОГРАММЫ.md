# Проблема: Программы Инсан не отображаются

## 🔍 Диагностика

### Проверка API

API работает корректно:
```bash
curl "https://fondinsan.ru/api/v1/programs?access-token=0xRs6obpvPOx4lkGLYxepBOcMju"
```

Ответ:
```json
{
  "success": true,
  "status": 200,
  "data": [
    {
      "id": 47,
      "title": "Помощь Хифз-центру",
      "url": "https://fondinsan.ru/project/47",
      ...
    },
    ...
  ]
}
```

### Проблема в коде

Программы загружаются, но **отображаются только если выбран партнер Инсан**.

В `partners.tsx`:
- Программы загружаются **всегда** (`useInsanPrograms()`)
- Но отображаются **только если** `isInsanPartner === true`
- `isInsanPartner` проверяет `selectedFund?.slug === 'insan'`

**Проблема**: Если партнер не выбран или slug не равен 'insan', программы не показываются.

---

## ✅ Решение

### 1. Улучшена проверка партнера

Добавлены дополнительные проверки для определения партнера Инсан:
- По slug: `'insan'`
- По ID: `'insan'`
- По названию: содержит `'инсан'` или `'ихсан'`

### 2. Добавлено логирование

Добавлен `console.log` для отладки:
- Какой партнер выбран
- Загружены ли программы
- Сколько программ

### 3. Улучшена обработка ответа API

Код теперь правильно обрабатывает формат ответа API:
```json
{
  "success": true,
  "status": 200,
  "data": [...]
}
```

---

## 🔧 Как проверить

1. Откройте приложение в браузере
2. Откройте DevTools (F12) → Console
3. Перейдите на страницу `/partners`
4. Выберите партнера Инсан (или перейдите по ссылке `/partners/insan`)
5. В Console должны появиться логи:
   ```
   [PartnersPage] Selected fund: { id: ..., name: ..., slug: ..., isInsanPartner: true }
   [PartnersPage] Insan programs: { count: 8, isLoading: false, data: [...] }
   [useInsanPrograms] Loaded programs: 8 [...]
   ```

6. Перейдите на вкладку "Проекты" (activeTab === "projects")
7. Должны отобразиться программы Инсан

---

## 🐛 Возможные проблемы

### Проблема 1: Партнер не определяется как Инсан

**Решение**: Проверьте в Console логи - какой slug у партнера. Если slug не 'insan', добавьте в код:
```typescript
const isInsanPartner = 
  selectedFund?.slug === 'insan' || 
  selectedFund?.id === 'insan' ||
  selectedFund?.name?.toLowerCase().includes('инсан') ||
  selectedFund?.name?.toLowerCase().includes('ихсан') ||
  partnerDetails?.data?.slug === 'insan' ||
  partnerDetails?.data?.name?.toLowerCase().includes('инсан');
```

### Проблема 2: Программы не загружаются (CORS)

**Решение**: 
- Проверьте Network tab - есть ли запросы к `fondinsan.ru/api/v1/programs`
- Если CORS блокирует - используйте Vercel Edge Function (см. `VERCEL_SETUP.md`)

### Проблема 3: Программы загружаются, но не отображаются

**Решение**:
- Проверьте Console логи - `insanPrograms.length`
- Проверьте что `isInsanPartner === true`
- Проверьте что `activeTab === "projects"`

---

## 📊 Ожидаемое поведение

1. **На странице `/partners`**:
   - Список всех партнеров
   - При клике на партнера → открывается детальная страница

2. **На странице `/partners/insan`**:
   - Информация о фонде Инсан
   - Вкладка "Проекты" → программы из API Инсан
   - Вкладка "О фонде" → информация о фонде

3. **Программы должны отображаться**:
   - Если выбран партнер Инсан
   - На вкладке "Проекты"
   - С картинками, описаниями, кнопками "Подробнее"

---

## ✅ Исправления применены

1. ✅ Улучшена проверка `isInsanPartner` - больше способов определения партнера
2. ✅ Добавлено логирование для отладки
3. ✅ Улучшена обработка ответа API - поддержка разных форматов
4. ✅ Программы всегда загружаются, даже если партнер не выбран

---

**Проверьте Console в браузере после обновления кода!**

