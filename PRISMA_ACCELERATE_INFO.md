# Prisma Accelerate Connection

## ✅ Что такое Prisma Accelerate?

**Prisma Accelerate** — это сервис для ускорения работы с базой данных через:
- Кэширование запросов
- Connection pooling
- Глобальное распределение
- Улучшенная производительность

## 🔗 Connection String

Ваш connection string использует формат:
```
prisma+postgres://accelerate.prisma-data.net/?api_key=...
```

Это означает, что запросы идут через Prisma Accelerate, а не напрямую к базе данных.

## ⚙️ Настройка

### 1. DATABASE_URL обновлён в .env

Connection string уже добавлен в `.env` файл.

### 2. Использование с Prisma Client

Prisma Client автоматически определит формат `prisma+postgres://` и будет использовать Accelerate.

### 3. Генерация Prisma Client

```bash
. "$HOME/.nvm/nvm.sh"
npx prisma generate
```

### 4. Применение миграций

**Важно**: Prisma Accelerate используется только для запросов через Prisma Client.

Для миграций нужно использовать **прямой connection string** к базе данных PostgreSQL, а не через Accelerate.

Если у вас есть прямой connection string к PostgreSQL, используйте его для миграций:

```bash
# Временно используйте прямой connection string для миграций
DATABASE_URL="postgresql://user:password@host:port/database" npx prisma migrate deploy
```

Или примените миграцию через Vercel SQL Editor.

## 📋 Проверка подключения

После установки зависимостей:

```bash
. "$HOME/.nvm/nvm.sh"
npm install
npx prisma generate
npm run db:test
```

## 🔒 Безопасность

⚠️ **Важно**: Connection string содержит API ключ. Не коммитьте его в Git!

Убедитесь, что `.env` файл в `.gitignore`:

```gitignore
.env
.env.local
.env.*.local
```

## 🎯 Преимущества Prisma Accelerate

1. **Кэширование**: Частые запросы кэшируются
2. **Connection Pooling**: Эффективное управление соединениями
3. **Глобальное распределение**: Близкие к пользователям серверы
4. **Мониторинг**: Аналитика запросов

## 📚 Документация

- **Prisma Accelerate**: https://www.prisma.io/docs/accelerate
- **Connection Strings**: https://www.prisma.io/docs/accelerate/connection-strings

