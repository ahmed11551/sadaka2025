# Инструкция: Настройка базы данных в Vercel

## 📋 Что делать в модальном окне

Вы видите окно **"Настроить sadaka2025"** для подключения базы данных Prisma.

### Шаг 1: Проверьте подключение
- ✅ База данных: `prisma-postgres-lime-house` (это ваша база данных Prisma)
- Это правильно, оставьте как есть

### Шаг 2: Выберите окружения (Окружающая среда)
- ✅ **Разработка** (Development) — для локальной разработки
- ✅ **Предварительный просмотр** (Preview) — для preview деплоев
- ✅ **Производство** (Production) — для production

**Рекомендация**: Оставьте все три галочки включёнными ✅

### Шаг 3: Пользовательский префикс
Сейчас указано: **"ХРАНИЛИЩЕ"**

**Рекомендация**: Измените на **"DATABASE"** или оставьте пустым

**Почему**: 
- Стандартное имя переменной будет `DATABASE_URL` (как в коде)
- Если оставить "ХРАНИЛИЩЕ", переменная будет `ХРАНИЛИЩЕ_URL`, и код не найдёт её

**Как изменить**:
1. Удалите текст "ХРАНИЛИЩЕ" из поля
2. Введите "DATABASE" (или оставьте пустым)
3. Переменная будет называться `DATABASE_URL`

### Шаг 4: Нажмите "Соединять"
После нажатия:
- Vercel создаст переменную окружения `DATABASE_URL` (или `ХРАНИЛИЩЕ_URL`, если не измените)
- База данных будет доступна во всех выбранных окружениях

---

## ✅ После подключения

### 1. Проверьте переменную окружения

В Vercel Dashboard:
1. Откройте проект `sadaka2025`
2. Перейдите в **Settings** → **Environment Variables**
3. Найдите `DATABASE_URL` (или `ХРАНИЩЕ_URL`)
4. Убедитесь, что она создана

### 2. Примените миграцию

После подключения БД нужно применить миграцию:

#### Способ 1: Через Vercel SQL Editor (рекомендуется)

1. В Vercel Dashboard откройте проект
2. Перейдите в раздел **Storage** или **Databases**
3. Найдите вашу базу данных `prisma-postgres-lime-house`
4. Откройте **SQL Editor** или **Query Tool**
5. Скопируйте содержимое файла `prisma/migrations/add_new_features.sql`
6. Вставьте в SQL Editor
7. Нажмите **Execute** или **Run**

#### Способ 2: Через Vercel CLI

```bash
# 1. Установите Vercel CLI (если ещё не установлен)
npm i -g vercel

# 2. Войдите в Vercel
vercel login

# 3. Подключите проект
vercel link

# 4. Получите DATABASE_URL
vercel env pull .env.local

# 5. Примените миграцию (если у вас установлен psql)
psql $(grep DATABASE_URL .env.local | cut -d '=' -f2) < prisma/migrations/add_new_features.sql
```

---

## ⚠️ Важно

### Если оставили префикс "ХРАНИЩЕ"

Если вы оставили префикс "ХРАНИЩЕ", переменная будет называться `ХРАНИЩЕ_URL` вместо `DATABASE_URL`.

**Что нужно сделать**:
1. В Vercel Dashboard → Settings → Environment Variables
2. Найдите `ХРАНИЩЕ_URL`
3. Либо переименуйте её в `DATABASE_URL`
4. Либо обновите код, чтобы использовать `ХРАНИЩЕ_URL`

**Рекомендация**: Лучше изменить префикс на "DATABASE" в модальном окне перед нажатием "Соединять"

---

## 📝 Итоговая последовательность действий

1. ✅ В модальном окне: измените префикс с "ХРАНИЩЕ" на "DATABASE" (или оставьте пустым)
2. ✅ Убедитесь, что все три окружения выбраны
3. ✅ Нажмите **"Соединять"** (Connect)
4. ✅ Дождитесь подключения
5. ✅ Примените миграцию через SQL Editor в Vercel
6. ✅ Проверьте, что переменная `DATABASE_URL` создана

---

## 🔍 Проверка после подключения

После подключения проверьте:

1. **Переменная окружения создана**:
   - Vercel Dashboard → Settings → Environment Variables
   - Должна быть `DATABASE_URL` с значением типа `postgresql://...`

2. **База данных доступна**:
   - Vercel Dashboard → Storage → prisma-postgres-lime-house
   - Должна быть видна информация о базе данных

3. **Миграция применена**:
   - Откройте SQL Editor
   - Выполните: `SELECT table_name FROM information_schema.tables WHERE table_name = 'payments';`
   - Должна вернуться строка `payments`

---

## 🆘 Если что-то пошло не так

### Ошибка подключения
- Проверьте, что база данных создана в Vercel
- Убедитесь, что у вас есть права доступа к проекту

### Переменная не создалась
- Перейдите в Settings → Environment Variables
- Создайте вручную: `DATABASE_URL` = значение из базы данных

### Не могу найти SQL Editor
- В Vercel Dashboard → Storage → выберите вашу БД
- Ищите вкладку "SQL" или "Query"
- Или используйте внешний клиент (pgAdmin, DBeaver)

