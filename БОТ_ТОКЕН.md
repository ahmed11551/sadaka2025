# Настройка токена Telegram бота

## ⚠️ ВАЖНО: Безопасность токена

**Токен бота**: `8387282083:AAEFiTQZHVJa3nMcqvh_FY-sVlcKq1N1fSA`

⚠️ **НИКОГДА не коммитьте токен в Git!**
⚠️ **Никогда не публикуйте токен в открытом доступе!**

---

## 📝 Где использовать токен

### 1. Локальная разработка (`.env` файл)

Создайте файл `.env` в корне проекта (если его нет):

```bash
# Скопируйте шаблон
cp .env.example .env
```

Добавьте токен в `.env`:

```env
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=8387282083:AAEFiTQZHVJa3nMcqvh_FY-sVlcKq1N1fSA
```

**Важно**: Файл `.env` уже в `.gitignore` - он не будет закоммичен.

### 2. Vercel (Environment Variables)

Добавьте токен в Vercel Dashboard:

1. Зайдите на https://vercel.com
2. Выберите проект `sadaka2025`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте новую переменную:
   - **Key**: `TELEGRAM_BOT_TOKEN`
   - **Value**: `8387282083:AAEFiTQZHVJa3nMcqvh_FY-sVlcKq1N1fSA`
   - Выберите: **Production**, **Preview**, **Development**
5. Нажмите **Save**
6. Пересоберите проект (Redeploy)

### 3. Backend (если используется)

Если вы используете backend для обработки команд бота или webhook, добавьте токен в backend `.env`:

```env
TELEGRAM_BOT_TOKEN=8387282083:AAEFiTQZHVJa3nMcqvh_FY-sVlcKq1N1fSA
```

---

## 🔧 Использование токена в коде

### Frontend (если нужно)

В React приложении токен обычно не нужен напрямую (Telegram WebApp работает без него). Но если нужно, используйте через env:

```typescript
const BOT_TOKEN = import.meta.env.VITE_TELEGRAM_BOT_TOKEN;
```

**Важно**: Префикс `VITE_` обязателен для Vite проектов!

### Backend (для webhook/команд)

В backend коде:

```typescript
const BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN;

// Пример использования для отправки сообщений
const response = await fetch(
  `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`,
  {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      chat_id: chatId,
      text: 'Привет!',
    }),
  }
);
```

---

## 🔐 Безопасность

### Что делать:

✅ Храните токен только в `.env` файлах  
✅ Используйте Environment Variables на Vercel  
✅ Добавьте `.env` в `.gitignore` (уже добавлен)  
✅ Используйте разные токены для dev/staging/production

### Чего НЕ делать:

❌ Не коммитьте токен в Git  
❌ Не публикуйте токен в открытом доступе  
❌ Не передавайте токен по незащищенным каналам  
❌ Не храните токен в коде приложения

---

## 🔄 Если токен скомпрометирован

Если токен попал в открытый доступ:

1. Немедленно отзовите токен через @BotFather:
   - Откройте @BotFather в Telegram
   - Отправьте `/revoke`
   - Выберите вашего бота
   - Подтвердите отзыв токена

2. Создайте новый токен:
   - @BotFather → `/mybots` → Ваш бот → **API Token**
   - Скопируйте новый токен

3. Обновите токен везде:
   - Локальный `.env`
   - Vercel Environment Variables
   - Backend `.env` (если используется)

---

## ✅ Чек-лист настройки

- [ ] Токен добавлен в локальный `.env` файл
- [ ] Токен добавлен в Vercel Environment Variables
- [ ] Токен добавлен в backend `.env` (если используется)
- [ ] `.env` файл в `.gitignore` (проверьте!)
- [ ] Токен НЕ закоммичен в Git
- [ ] Проект пересобран после добавления токена
- [ ] Бот работает корректно

---

**Токен бота готов к использованию!** 🚀

