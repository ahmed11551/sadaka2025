# Альтернативные способы установки зависимостей

## 🔧 Проблема

Сетевые ошибки (`ECONNRESET`) при установке через `npm install`.

## ✅ Альтернативные решения

### Вариант 1: Использовать Yarn (рекомендуется)

Yarn часто лучше работает с нестабильным интернетом:

```bash
. "$HOME/.nvm/nvm.sh"

# Установить Yarn глобально
npm install -g yarn

# Использовать Yarn для установки
yarn install
```

**Преимущества:**
- Лучшая обработка сетевых ошибок
- Кэширование пакетов
- Параллельная установка

---

### Вариант 2: Использовать pnpm

pnpm - быстрый и эффективный менеджер пакетов:

```bash
. "$HOME/.nvm/nvm.sh"

# Установить pnpm
npm install -g pnpm

# Использовать pnpm
pnpm install
```

**Преимущества:**
- Экономия места на диске
- Быстрая установка
- Хорошая обработка ошибок

---

### Вариант 3: Установить только критичные пакеты

Установить только то, что нужно для MongoDB:

```bash
. "$HOME/.nvm/nvm.sh"

# Только MongoDB и минимальные зависимости
npm install mongodb dotenv --no-save --legacy-peer-deps

# Или по одному
npm install mongodb
npm install dotenv
```

---

### Вариант 4: Использовать Docker (если установлен)

Создать Dockerfile с предустановленными зависимостями:

```dockerfile
FROM node:24-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --registry=https://registry.npmmirror.com
COPY . .
CMD ["npm", "run", "dev"]
```

Запуск:
```bash
docker build -t sadaka2025 .
docker run -p 5000:5000 sadaka2025
```

---

### Вариант 5: Использовать готовый package-lock.json

Если есть `package-lock.json`, можно попробовать:

```bash
. "$HOME/.nvm/nvm.sh"

# Очистить кэш
npm cache clean --force

# Установить с использованием lock файла
npm ci
```

---

### Вариант 6: Ручная установка через другой компьютер

1. На компьютере с рабочим интернетом:
   ```bash
   npm install
   tar -czf node_modules.tar.gz node_modules/
   ```

2. Перенести `node_modules.tar.gz` на этот компьютер
3. Распаковать:
   ```bash
   tar -xzf node_modules.tar.gz
   ```

---

### Вариант 7: Использовать VPN или прокси

Если проблема с доступом к npm registry:

```bash
# Настроить прокси (если есть)
npm config set proxy http://proxy.example.com:8080
npm config set https-proxy http://proxy.example.com:8080

# Или использовать VPN
npm install
```

---

### Вариант 8: Установить пакеты по одному с задержкой

```bash
. "$HOME/.nvm/nvm.sh"

# Установить критичные пакеты по одному
npm install mongodb
sleep 5
npm install dotenv
sleep 5
npm install express
# и т.д.
```

---

### Вариант 9: Использовать локальный npm registry

Если есть доступ к другому npm registry:

```bash
npm config set registry http://localhost:4873  # пример локального registry
npm install
```

---

### Вариант 10: Проверить и исправить сетевые настройки

```bash
# Проверить DNS
nslookup registry.npmjs.org

# Очистить DNS кэш (macOS)
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder

# Попробовать снова
npm install
```

---

## 🎯 Рекомендуемый порядок попыток

1. **Yarn** (самый надёжный):
   ```bash
   npm install -g yarn
   yarn install
   ```

2. **pnpm**:
   ```bash
   npm install -g pnpm
   pnpm install
   ```

3. **Только критичные пакеты**:
   ```bash
   npm install mongodb dotenv --no-save
   ```

4. **Docker** (если установлен)

5. **Ручная установка** с другого компьютера

---

## 📋 Быстрая проверка после установки

После успешной установки зависимостей:

```bash
# Проверить подключение к MongoDB
npm run db:mongo:test

# Создать индексы
npm run db:mongo:indexes

# Запустить сервер
npm run dev
```

---

## 💡 Самый простой способ (если есть другой компьютер)

1. Скопировать проект на компьютер с рабочим интернетом
2. Выполнить `npm install` там
3. Скопировать `node_modules/` обратно

---

**Попробуйте Yarn - это самый надёжный вариант!** 🚀

