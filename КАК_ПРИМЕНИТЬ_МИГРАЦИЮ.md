# Как применить миграцию БД

## 🎯 Быстрый способ

### Если у вас установлен Node.js:

```bash
# 1. Установите зависимости (если ещё не установлены)
npm install

# 2. Примените миграцию
npx prisma migrate deploy
```

### Если у вас установлен PostgreSQL клиент (psql):

```bash
# Примените SQL файл напрямую
psql $DATABASE_URL < prisma/migrations/add_new_features.sql
```

### Через скрипт (автоматический выбор способа):

```bash
# Запустите скрипт
./apply-migration.sh
```

---

## 📋 Детальные инструкции

### Вариант 1: Через Prisma (рекомендуется)

**Требования**: Node.js установлен

```bash
# 1. Убедитесь, что зависимости установлены
npm install

# 2. Проверьте, что DATABASE_URL настроен
# В .env файле или как переменная окружения

# 3. Примените миграцию
npx prisma migrate deploy
```

**Что делает**: Prisma автоматически применяет все неприменённые миграции из папки `prisma/migrations/`

---

### Вариант 2: Через SQL напрямую

**Требования**: PostgreSQL клиент (psql) установлен

```bash
# 1. Убедитесь, что DATABASE_URL настроен
export DATABASE_URL="postgresql://user:password@host:port/database"

# 2. Примените SQL файл
psql $DATABASE_URL < prisma/migrations/add_new_features.sql
```

**Что делает**: Выполняет SQL команды из файла напрямую в базе данных

---

### Вариант 3: Через Docker

**Если используете Docker Compose:**

```bash
# 1. Запустите контейнеры
docker-compose up -d postgres

# 2. Примените миграцию в контейнере
docker-compose exec postgres psql -U sadakapass -d sadakapass < prisma/migrations/add_new_features.sql
```

---

### Вариант 4: Через графический интерфейс

**Используя pgAdmin, DBeaver, TablePlus или другой SQL клиент:**

1. Откройте ваш SQL клиент
2. Подключитесь к базе данных
3. Откройте файл `prisma/migrations/add_new_features.sql`
4. Скопируйте весь SQL код
5. Выполните в SQL клиенте

---

## ⚠️ Важно перед применением

1. **Сделайте backup базы данных**:
   ```bash
   pg_dump $DATABASE_URL > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Проверьте DATABASE_URL**:
   ```bash
   echo $DATABASE_URL
   # Или проверьте в .env файле
   ```

3. **Убедитесь, что база данных доступна**:
   ```bash
   psql $DATABASE_URL -c "SELECT version();"
   ```

---

## ✅ Проверка после применения

После успешного применения миграции проверьте:

```sql
-- Проверить наличие таблицы payments
SELECT table_name FROM information_schema.tables WHERE table_name = 'payments';

-- Проверить наличие колонки role в users
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'role';

-- Проверить наличие колонки moderationStatus в campaigns
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'campaigns' AND column_name = 'moderationStatus';

-- Проверить наличие таблицы zakat_calculations
SELECT table_name FROM information_schema.tables 
WHERE table_name = 'zakat_calculations';
```

Если все запросы возвращают результаты — миграция применена успешно! ✅

---

## 🐛 Решение проблем

### Ошибка: "command not found: npx"

**Решение**: Установите Node.js
```bash
# macOS
brew install node

# Или скачайте с https://nodejs.org/
```

### Ошибка: "command not found: psql"

**Решение**: Установите PostgreSQL клиент
```bash
# macOS
brew install postgresql

# Ubuntu/Debian
sudo apt-get install postgresql-client
```

### Ошибка: "DATABASE_URL не найден"

**Решение**: Создайте `.env` файл или установите переменную окружения
```bash
# В .env файле
DATABASE_URL=postgresql://user:password@host:port/database

# Или экспортируйте
export DATABASE_URL="postgresql://user:password@host:port/database"
```

### Ошибка: "relation already exists"

**Решение**: Миграция уже применена. Это нормально, можно игнорировать.

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи ошибок
2. Убедитесь, что DATABASE_URL правильный
3. Проверьте права доступа к базе данных
4. См. `ИНСТРУКЦИЯ_ПРИМЕНЕНИЯ_МИГРАЦИИ.md` для деталей

